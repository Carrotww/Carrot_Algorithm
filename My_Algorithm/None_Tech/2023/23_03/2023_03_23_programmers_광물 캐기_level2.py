# https://school.programmers.co.kr/learn/courses/30/lessons/172927

# 푸는 중
def solution(picks, minerals):
    cur_pick = [-1, 0]
    
    for min in minerals:
        if cur_pick[1] == 0:
            if sum(picks) == 0:
                break
            if min == 'diamond':
                
            
            
        if min == 'diamond':